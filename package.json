{
  "name": "flat-route-finder",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "One-tap flat running route finder — pick a distance, get a flat loop from your location.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
